# 🖥️ Jarvis Desktop Application Setup\n\n## Prerequisites\n\n- **Node.js 14+** - Download from https://nodejs.org/\n- **Python 3.9+** - Should already be installed\n- **npm** - Comes with Node.js\n\n## 🚀 Quick Start (3 Simple Steps)\n\n### Step 1: Install Dependencies\n\n```bash\n# In the root directory (jarvis-assistant/)\nnpm install\n```\n\nThis installs Electron and all Node dependencies.\n\n### Step 2: Setup Python Environment\n\n```bash\ncd backend\npip install -r requirements.txt\ncp .env.example .env\n```\n\nEdit `.env` and add your OpenAI API key:\n```\nOPENAI_API_KEY=your_api_key_here\n```\n\n### Step 3: Run Jarvis!\n\n```bash\ncd ..\nnpm run dev\n```\n\n**That's it!** 🎉 Jarvis will:\n1. Start the Python backend\n2. Launch as a desktop window\n3. Be ready to use!\n\n---\n\n## 📦 Creating Installers\n\nAfter testing with `npm run dev`, you can create professional installers:\n\n### Windows (.exe)\n\n```bash\nnpm run build-win\n```\n\nCreates:\n- `dist/Jarvis AI Assistant Setup 1.0.0.exe` - Full installer\n- `dist/Jarvis AI Assistant 1.0.0.exe` - Portable (no install)\n\n### macOS (.dmg)\n\n```bash\nnpm run build-mac\n```\n\nCreates: `dist/Jarvis AI Assistant-1.0.0.dmg`\n\n### Linux (.AppImage)\n\n```bash\nnpm run build-linux\n```\n\nCreates: `dist/Jarvis AI Assistant-1.0.0.AppImage`\n\n### All Platforms\n\n```bash\nnpm run build\n```\n\n---\n\n## ⚙️ What Happens When You Run `npm run dev`\n\n1. 🐍 **Python backend starts** on `http://localhost:8000`\n2. ⏳ **Waits** for backend to be ready (3-5 seconds)\n3. 🪟 **Electron window opens** with Jarvis UI\n4. 🤖 **Jarvis is ready** to use!\n\nAll in one command! ✨\n\n---\n\n## 🐛 Troubleshooting\n\n### \"npm: command not found\"\n\nNode.js is not installed. Download from https://nodejs.org/ and install it.\n\n### \"Python not found\"\n\n```bash\n# If python3 is installed:\nset PYTHON_EXE=python3  # Windows\nexport PYTHON_EXE=python3  # macOS/Linux\nnpm run dev\n```\n\n### \"Port 8000 already in use\"\n\nAnother Jarvis instance is running:\n\n```bash\n# Windows\nnetstat -ano | findstr :8000\ntaskkill /PID <PID> /F\n\n# macOS/Linux\nlsof -i :8000\nkill -9 <PID>\n```\n\n### Backend fails to start\n\nCheck the backend directly:\n```bash\ncd backend\npython app.py\n```\n\nLook for error messages and fix them.\n\n### \"electron not found\"\n\nMake sure you ran `npm install`:\n```bash\nnpm install\n```\n\n---\n\n## 📁 Project Structure\n\n```\njarvis-assistant/\n├── main.js              ← Electron main process\n├── preload.js           ← Security layer\n├── package.json         ← Node dependencies\n├── backend/\n│   ├── app.py          ← Python FastAPI server\n│   └── requirements.txt\n├── frontend/\n│   ├── index.html\n│   ├── css/\n│   └── js/\n└── assets/\n    └── icon.png         ← App icon\n```\n\n---\n\n## 🎯 Next Steps\n\n1. ✅ Install Node.js\n2. ✅ Run `npm install`\n3. ✅ Configure `.env` with OpenAI key\n4. ✅ Run `npm run dev` to test\n5. ✅ Run `npm run build-win` (or your OS) to create installer\n6. ✅ Share the installer with others!\n\n---\n\n## 💡 Tips\n\n- **Development**: `npm run dev` includes hot reload\n- **Production**: Installers are in the `dist/` folder after building\n- **Icons**: Replace `assets/icon.png` with your custom icon\n- **Branding**: Edit `package.json` to customize app name\n\n**Enjoy your Jarvis desktop app!** 🚀\n